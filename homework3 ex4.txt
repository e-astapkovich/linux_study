4.

ЗАДАНИЕ
Установка node из исходников
- склонировать репозиторий https://github.com/nodejs/node в /opt/.
- установить компилятор и необходимые зависимости, как написано в https://github.com/nodejs/node/blob/master/BUILDING.md#building-nodejs-on-supported-platforms
- скомпилировать node (это займет какое-то время). Тесты прогонять никакие не нужно!
- заставить бинарь node работать в терминале при вводе node из любого каталога в системе.



РЕШЕНИЕ:

склонировал
сначала не удалось, т. к. папка /opt принадлежит руту, а у остальных нет прав на запись в нее.
выполнил с sudo (не думал, что потом снова возникнут сложности с доступом...)
	$ sudo git clone https://github.com/nodejs/node.git /opt/nodejs
	$ sudo apt-get update
	$ sudo apt-get install python g++ make
	$ sudo apt update
	$ sudo apt install python3-distutils -y
	$ ./configure
Последняя команда не выполнилась: Permission denied: 'icu_config.gupi'
И я понял, что проблемы с доступом при установке еще могут появиться...
Воспользовался вашей подсказкой с урока:
Установил для директории nodejs в качестве группы-владельца свою основную группу.
	$ sudo chown -R root:eastapkovich /opt/nodejs
Рекурсивно, чтоб группа-владелец сменилась и у вложенных папок и файлов, т.к. репозиторий уже был склонирова
И установил для директроии SGID
	$ sudo chmod g+s /opt/nodejs

Но это не помогло... я не разобрался почему...
выполнил $ sudo ./configure

